


h3. Stuffs to search or to make myself

* Ant property helper to access Java Enums or final static field.
* Utility to inspect duplicates in classpath in terms of Class, Resource, Archive or so on.
* Wiki extension which can create in-situ or on-the-fly variable.
* Apache plug-in : completion window



h3. Applications or libraries to make

* Finance/stock data application
** @thirdstage.finance.stock@

* Library for widget meta in JSON
** @thirdstage.dv.widget.meta@

* Library for message conversion - 
** @thirdstage.mcon@

* Library for JSON based configuration
** @thirdstage.util.config.json@

* Chart and graph library based on D3.js

* Dashboard application based on JavaScript

* JSON based configuration class
** support array
** support meta for item
*** datatype, default, description, value constraints 

* Default supproting Property wrapper or management class
** String getProperty(String key)
** String getDefaultProperty(Strint key)
** Property getProperties()
** Property getDefaultProperties()
** Set<String> getOveridenPropertyKeys()
** boolean isOverriden(String key)
** > refer Apache Commons Configuration

* Bootstrap template or generator : Startup or QuickStart, EasyStart
** Generate default projects, package structure, project configuration, spring configuration, ...
** Apply Maven, Eclipse
** Script or configuration based Eclipse installation.
** Provide local testing environment as possible
** Build server environment for testing
** Generate web appl. with jQuery, Twitter Bootstrap, HTML5 Boilerplate, KnockOutJS and more.
** Provide sample application and documentation.