class AlphaNumericCharacter
!!!9858587.java!!!	isValidCharacter(in aChar : char) : boolean
            return (Character.isLetterOrDigit(aChar) &&
                     super.isValidCharacter(aChar));
