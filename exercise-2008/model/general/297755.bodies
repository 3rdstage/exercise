class SpecialMethod
!!!1615003.java!!!	getSpecialMethod(in operation : String) : SpecialMethod
	for(int i = 0; i < methods.length; i++)
	    if (methods[i].getName().equals(operation))
		return methods[i];
	return null;
