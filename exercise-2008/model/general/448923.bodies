class Actions
!!!2738331.java!!!	Actions(in key : String)
            super(key);
!!!2738459.java!!!	actionPerformed(inout e : ActionEvent) : void
            if (getName() == CLOSE) {
                JOptionPane optionPane = (JOptionPane)e.getSource();

                optionPane.setValue(new Integer(JOptionPane.CLOSED_OPTION));
            }
