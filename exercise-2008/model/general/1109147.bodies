class LinkVector
!!!8939163.java!!!	baseElementIndex(inout e : Element) : int
		HTMLLink l;
		for (int i = 0; i < elementCount; i++) {
		    l = (HTMLLink) elementAt(i);
		    if (l.element == e) {
			return i;
		    }
		}
		return -1;
