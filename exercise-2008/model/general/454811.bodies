class ModelListener
!!!2790555.java!!!	stateChanged(inout e : ChangeEvent) : void
	    if (!useCachedValue) {
		scrollBarValue = scrollbar.getValue();
	    }
	    layoutContainer(scrollbar);
	    useCachedValue = false;
