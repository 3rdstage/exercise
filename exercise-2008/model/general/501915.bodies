class parse_reduce_row
!!!3092379.java!!!	parse_reduce_row()
      /* make sure the size is set */
      if (_size <= 0 )  _size = non_terminal.number();

      /* allocate the array */
      under_non_term = new lalr_state[size()];
!!!3092507.java!!!	size() : int
return _size;
