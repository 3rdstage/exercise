class DefaultRegionContainment
!!!9972635.java!!!	sharedInstance() : DefaultRegionContainment
	    if (si == null) {
		si = new DefaultRegionContainment();
	    }
	    return si;
!!!9972763.java!!!	contains(in x : int, in y : int, in width : int, in height : int) : boolean
	    return (x <= width && x >= 0 && y >= 0 && y <= width);
