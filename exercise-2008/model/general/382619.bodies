class ModelListener
!!!2210971.java!!!	stateChanged(inout e : ChangeEvent) : void
	  if (isShowing()) {  // isVisible
	        updateChooser();
		dirty = false;
	    } else {
	        dirty = true;
	    }
