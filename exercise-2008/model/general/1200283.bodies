class BRView
!!!9921307.java!!!	BRView(inout elem : Element)
	super(elem);
!!!9921435.java!!!	getBreakWeight(in axis : int, in pos : float, in len : float) : int
	if (axis == X_AXIS) {
	    return ForcedBreakWeight;
	} else {
	    return super.getBreakWeight(axis, pos, len);
	}
