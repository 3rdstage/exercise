
h3. Design

h4. Oozie Workflow Editor

* Feature
** *tag and attribute proposal*
** *autocomplete*
*** closing tags
** *formatting* (pretty printing)
*** indentation
*** attribute value normalization
** *checking well-formedness*
** *validating*
*** schema based validation
*** IDENTIFIER type attributes
*** uniqueness of action names
*** consistency of 'from', 'to', and 'name' attriubte values
** *snippet*
** *EL support*
** *string normalization* (when formatting)
*** @name@ element text
*** @IDENTIFIER@ type value
** *folding*
** *wrapping*
*** soft wrapping
*** line number
** *area resizing*
** *current line highlighting*
** *sample view*
** *syntax view*

* Main category of feature
** *Coding assist*
*** hint or proposal
*** snippet
** *Formatting*
*** string normalization
*** indentation
*** line break
*** wrapping
** *Validation*
*** well-formedness
*** valiation on Schema


* Options
** boolean normalizesValueElement
** boolean appliesFolding
** boolean addsClosingTags
** int fontSize
** boolean usesTab
** int indentSize
** selection version

* Layout
** Commands
*** format
*** validate
*** undo
*** redo
*** copy
*** paste
** Options
** Message panel
** Snippet panel
** Editing panel
** Short-cuts

* Tools
** CodeMirror
** "jQuery.parseXML()":http://api.jquery.com/jQuery.parseXML/
** Regex

h4. Considerations

* *Folding excluded tags*
** Add feature or option to define *excluded* tags.

* *The cursor position after formatting*

* *Normalize attribute values*

* *Regex based normalization of tags and attributes*

* *Token based normalization of tags and attributes*
** What if the document is not in well-formed states?

* *Tag normalization at the typing of '>'*
** What about CDATA

* *Selection behavior*
** single click, double click, triple click inside tags
** at tag name, at attribute name, at attribute value.

* *More active usage of @token@ and @state@*
** How to access current token?
** How to access current state and surrounding context?

* *Various code folding*
** Fold all : unfold only child of workflow-app element
** Unfold all
** Fold to depth n : 

* *Folding free tags*
** @property@

* *Show hint or proposal with F2 key*

* *Single @backspace@ to proper indented position*


 