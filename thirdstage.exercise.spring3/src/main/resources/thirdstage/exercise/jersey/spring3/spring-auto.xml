<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context" xmlns:jdbc="http://www.springframework.org/schema/jdbc"
	xmlns:p="http://www.springframework.org/schema/p" xmlns:tx="http://www.springframework.org/schema/tx" xmlns:util="http://www.springframework.org/schema/util"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.0.xsd
		http://www.springframework.org/schema/jdbc http://www.springframework.org/schema/jdbc/spring-jdbc-3.0.xsd
		http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-3.0.xsd
		http://www.springframework.org/schema/util http://www.springframework.org/schema/util/spring-util-3.0.xsd">

	<description>Samples for Jersey and Spring</description>

	<!-- @todo make the upper element work correctly
	<context:property-placeholder location="#{systemProperties['bootstrap.configLocation']}" /> -->
	<context:property-placeholder location="classpath:thirdstage/exercise/jersey/spring3/config.properties"/>


	<!-- Makes activate Spring annotations such as @Required, @Autowired, @Resource et al -->
	<context:annotation-config />


	<!-- Exports fabric components explicitly -->
	<!-- @todo 
		candidates to export : SLF4J main, Log4j main, this Spring context, Jetty server
	-->
	<bean id="jmxExporter" class="org.springframework.jmx.export.MBeanExporter" lazy-init="false">
		<property name="beans">
			<map>
				<entry key="${jmx.domain}:type=fabric,name=dataSource" value-ref="dataSource"/>
			</map>
		</property>
	</bean>


	<bean id="dataSource" class="org.apache.commons.dbcp.BasicDataSource">
		<property name="driverClassName" value="${datasource.jdbcDriver}" />
		<property name="url" value="${datasource.jdbcUrl}" />
		<property name="username" value="${datasource.user}" />
		<property name="password" value="${datasource.password}" />
		<property name="defaultAutoCommit" value="${datasource.defaultAutoCommit}" />
		<property name="maxActive" value="${datasource.maxActive}" />
		<property name="maxIdle" value="${datasource.maxIdle}" />
		<property name="minIdle" value="${datasource.minIdle}" />
		<property name="initialSize" value="${datasource.initSize}" />
	</bean>


	<!-- load domain components (JAX-RS resources and services) for this application -->
	<context:component-scan base-package="thirdstage.exercise.jersey.spring3" />

</beans>
